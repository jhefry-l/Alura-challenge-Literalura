package valverde.com.mx.literalura.repositories;

import valverde.com.mx.literalura.models.LanguageT;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LanguageRepository extends JpaRepository<LanguageT, Integer> {
}
